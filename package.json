{
  "name": "coingecko-crypto-scraper",
  "version": "1.0.0",
  "description": "Apify Actor that scrapes cryptocurrency market data from the CoinGecko API",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "start": "node dist/main.js",
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "keywords": ["apify", "coingecko", "crypto", "cryptocurrency", "prices", "scraper"],
  "license": "Apache-2.0",
  "dependencies": {
    "apify": "^3.2.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
