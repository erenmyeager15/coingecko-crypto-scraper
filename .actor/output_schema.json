{
  "actorOutputSchemaVersion": 1,
  "title": "CoinGecko Crypto Scraper Output",
  "description": "Structured cryptocurrency market records from the official CoinGecko API, plus machine-readable run diagnostics.",
  "type": "object",
  "properties": {
    "results": {
      "type": "string",
      "title": "CoinGecko market records",
      "description": "Normalized coin identity, price, market-cap, volume, supply, ATH/ATL, source URL, attribution, and update fields.",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    },
    "runStatus": {
      "type": "string",
      "title": "Run status and diagnostics",
      "description": "Outcome, request/retry/rate-limit counters, search and record counts, partial-failure diagnostics, and spending-limit state.",
      "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/RUN_STATUS"
    }
  }
}
